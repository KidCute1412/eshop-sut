# AI Critique (200-300 words)

Write one paragraph, 200-300 words, addressing all three of:

1. Where did the AI get something wrong, biased, or incomplete? (Cite a concrete instance from
   `ai-gap-analysis.md` — a specific fragile selector, a specific weak/missing assertion, a specific
   missed edge case, or a specific flaky wait.)
2. Why did it fail to catch the issue? (Prompt quality / model limitation / a characteristic of this
   specific EShop page — e.g. the total absence of `data-testid`/`id` attributes — that only became
   apparent by actually reading the rendered DOM or running the script.)
3. What principle have you learned about collaborating with AI during this assignment?

---

TODO: draft here, then trim to 200-300 words.

Word count: TODO
