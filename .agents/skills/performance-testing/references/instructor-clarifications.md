# Instructor Clarifications and Interpretation Notes

- Treat `HW05_Performance_Testing_En.md` as the grading source for deliverables.
- Treat `README.md` as the business requirement oracle.
- Treat `setup_guide.md` as the startup and environment source.
- Treat `api_specification.md` as the backend API contract.
- Do not claim performance results without raw execution evidence.
- If a tool cannot be run locally, produce reproducible plans and explicitly mark execution evidence
  as pending.
- HW05 says "as completely as possible", but missing required documents can still score 0; separate
  finished design artifacts from pending real-run evidence.
- The assignment is individual, but also says workflow selection should not duplicate group members;
  record the student's confirmation instead of silently resolving this ambiguity.

