FR-05: Product listing and search

The Equivalence classes generated by the agent has many flaws. The main reasoning for this is due to the majority of tests for FR-05 being not applicable for boundary value analysis, as well as many sub-requirements under FR-05 to not require a partition. The list of equivalence classes (ECs) generated have been viewed, modified and adjusted by the tester, specifically:
- Removed ECs where the "partition" are simple UI checks.
- Corrected the ECs to indicate no usage of boundary value analysis is applicable here.
- Readjusted input values to actually match products within the database.
- Added an additional EC for Unicode search terms (like "Bàn phím").
- Added ECs for outputs (matching, empty, full products) as they were missing.
- Specified boundary value analysis for white space query (>= 0 white spaces), leading to 2-point boundary values for testing ("" and " ")
-> This is possibly due to the instructions heavily implying both the usage of both Domain Testing and Boundary Value Analysis (BVA), leading to it hallucinating the effect of BVA on all ECs. Since the agent does not currently have access to the UI, as well as missing the context that the website has Vietnamese products, the EC and test case for this purpose is missing.

Similarly, the test cases are readjusted to match the correct equivalence classes while simlar tests are created for the new ECs. Full details can be found within the traceability matrix and test table. 4 test cases are also merged into 2: loading screen observer and an invalid search, empty message and a blank search.
-> This is due to the test merging condition is only conditioned for domain testing, rather than all tests in general.

Minor error, when displaying HTML elements as text, "&amp;lt;" is used to replace "<" in HTML brackets, otherwise they will be not appear.
-> This is a small error done by the agent.

FR-14: Category management (CRUD) 
Since this requirement is very short, the small amount of tests and ECs generated by the agent make sense, however, there are some errors needing fix and some adjustments:
- White-space inputs can be grouped together like in FR-05 under one class as an instance of BVA.
- Added one extra valid class for Unicode.
- Added 3 new classes for outputs which were previously missing from the generated classes.
- Readjust the information so that truthfully, BVA is only applied on one class (white-space).
-> Same possible reasoning as in FR-05, as BVA was too heavily implied, with missing context of Vietnamese leading to missing the Unicode case. The missing outputs class could be due to the agent skill not providing enough context that the output is also applicable for equivalence partitioning.


FR-11, FR-15 and FR-21 did not involve usage of an agent skill, the reasoning is due to the monthly credit limit.