# Cross-platform screenshots go here

Naming convention: `[platform]_[screen].png`, e.g. `chrome_home.png`, `firefox_checkout.png`, `expogo_cart.png`.

Every file must show the browser/OS/device chrome (so the platform is verifiable) plus the localhost/tunneled SUT URL, and must have `23127296@hcmus.edu.vn` overlaid on the image per the assignment's anti-cheat requirement (§11).
