# Bug screenshots go here

Name each file `BUG-XX.png` (e.g. `BUG-11.png`) to match the Bug ID in `../Bug_Report.md`.

These must be real screenshots taken while reproducing each bug against a running instance of the SUT — see `../HOWTO_create_github_issues.md`.
